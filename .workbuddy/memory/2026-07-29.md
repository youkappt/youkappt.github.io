# 2026-07-29 工作日志

## ppthub「动手试试 + 改前改后」全量收官

按用户指令「先把文档和记忆整理一下（neat-freak 已在前序完成），把剩下的术语都做了，改前改后也一起做了」，今日完成 ppthub 全部 184 术语三件套 100% 覆盖。

### 关键动作
1. **修复 beforeafter.js 损坏**：前序「输出与放映」子代理多遍正则叠加，导致 3 套重复 BA 块（@280-351）+ 语法错误（@299 Unexpected token ')'）。手工删 2 套重复、保留 1 套，并把 fill-rate/bleed 的 `.replace()` hack 改为 `slide(inner,'padding:0')`。node --check 通过。
2. **修复「输出与放映」子代理损坏**：19 个 demo 签名损坏（缺闭引号/缩写冲突）→ 单遍 Python 重命名 + 修 data-m 冲突；6 个母版函数重复（旧版无案例 + 案例化新版）→ 删旧留新；combo-chart 从线上基线 `youkappt.com/ppthub/demos.js` 恢复函数。
3. **三件套批量补齐**（含之前完成的 设计原则15/图形23/动画16/图片15/输出21）：
   - 效率与技巧 14 + combo-chart：用 CASES 范式重写为案例版（子代理，验证函数总数 183→184、15 个含「案例：」、verify 13项全过）。
   - BA 视觉图补齐 63（并行子代理，单遍插 `/* __BA_APPEND__ */` 前；验证 181 键、63 目标全在、渲染 0 异常、锚点唯一）。
   - beforeafter 文字补齐 41（并行子代理，单遍插字段补逗号；验证 41/41 含字段、语法 OK）。
4. **全量门禁**：node --check 三文件全过；verify_ppthub 13/13（184 术语）；jsdom runtime 184 demo 仅 ink(canvas 限制) 预期失败；重新盘点 181 术语(去 3 分类节点) 的 demo/BA/文字 缺口均清零。

### 验证结论
- 184 术语全部有案例化 demo；181 术语(去分类节点) BA 视觉图 + beforeafter 文字 100% 覆盖。
- 本地预览：http://localhost:8055 （python http.server --directory ppthub，pid 92732）。**未部署线上**（用户仅授权本地改动）。

### 经验教训再确认
- 子代理批量改 demos.js 易触发多遍正则叠加/漏逗号损坏 → 必须单遍脚本 + 每次 node --check + 我侧独立验证（已两次抓出谎报/损坏）。
- beforeafter.js 的 `/* __BA_APPEND__ */` 锚点单遍替换是最安全的新增方式，绝不动现有内容。

## 设计原则 7 demo 从「抽象线框」重写为「真实 PPT 幻灯片风格」

用户反馈截图：repetition demo 内容几乎不可见（64px 小卡片 + #c2cfe0 浅色字）。根因：全部 7 个设计原则 demo（alignment/proximity/contrast/repetition/hierarchy/whitespace/focus）都用抽象极简风渲染，不像 PPT。

### 修复内容
- 逐个替换 7 个 demo 函数，新版本特征：
  - 完整 slide 结构（顶部强调色条 #2f6f3a + 标题区 + 内容区 + 页脚）
  - 深色可读文字 #1a3300 / #4a5a3a（不再用 #c2cfe0 近不可见色）
  - CASES 真实中文内容（标题/要点/副标题/页脚）
  - on/off 状态对比清晰（如 repetition「各不同」= 不同色/大小/圆角 vs 「统一重复」= 统一样式卡片）
  - 用字符串拼接 (+) 禁嵌套模板字符串（防白屏）
- node --check 通过，本地预览 http://localhost:8021 就绪。**未部署线上**。

## 行距 demo 修复：单段文字 → 多行内容

用户反馈：行距 demo 只有一行 `cs.body`，拖滑块看不出变化。
- 改为多行结构：标题 + 绿色强调线 + 4 条要点（• 前缀）+ 正文段落（分隔线隔开）
- 幻灯片容器加顶部色条，更像真实 PPT 风格
- node --check 通过。**未部署线上**。

## Morph（切换）demo 重写：单方块挪位 → 真实幻灯片切换

用户反馈：只有一个色块从 A 挪到 B，加载完就播完了，看不出「平滑」vs「生硬」区别。
- 重写为 Slide A（内容页：标题+要点）→ Slide B（章节页：PART 大号）的幻灯片切换
- 三个按钮可重复播放：「平滑 Morph」（淡入+缩放 0.65s）vs「生硬直接跳」（无动画）vs「重播」
- 底部提示文字随模式变化说明差异
- node --check 通过。**未部署线上**。

## 改前改后 BA_VISUALS 白底不可见修复

- **根因**：styles.css reskin 浅色模式（~L1033）把 `.ba-slide` 从深底 `rgba(26,51,0,0.55)` 覆盖为 `#ffffff` 白底，但 beforeafter.js 全部 180+ 个 BA_VISUALS 文字颜色（`#c2cfe0`/`#7e8aa0`/`#56657f`）都是配深底的浅色字 → 白底全不可见
- **修复**：`.ba-slide` 背景改为 `#1e1e2e`（暗海军蓝），保持深底让现有颜色正常工作，无需逐个改 BA_VISUALS
- 用户截图术语为 autoplay（自动播放），但影响全部有 BA_VISUALS 的术语
