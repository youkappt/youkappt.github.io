# 2026-08-03 工作日志

## 战略模型画布收尾与验证

- 用 Chrome headless + chrome-remote-interface 对 `strategy-models/` 全部 9 个模型逐个截图验证（画布 `.cm-stage` 1280×857，共 10 张含安索夫长文字压力测试）。
- 验证结果：波特五力、价值链、钻石、BCG 矩阵、经验曲线、安索夫矩阵、杜邦分析、标杆分析、德鲁克七源均渲染正确；安索夫矩阵 q1 灌入 25 段重复文字后，矩阵 2×2 结构未变形，文本框内部滚动。
- 本地 commit：`4474c0b`「feat: 新增战略模型画布子页 strategy-models」，仅包含 `strategy-models/` 目录与 `docs/plans/2026-07-31-strategy-models-design.md`，未混入用户之前未提交的 `tools.md` 大改。
- 运行 `tests/verify_before_deploy.py`：**17/17 PASS**，允许上线。
- 未部署，待用户明确「部署上线」指令。

## 将 SceneMint 加入优卡导航

- 按用户要求把 `https://image.sunls.de/`（SceneMint，sunls 用 AI 做的免登录文生图网站，免费使用、签到得生成额度）加入 `tools.md`。
- 归类到 `## AI生图 (IMAGE)` → `### 生图工具 (TOOLS)` 子分类（即梦AI 之后），标注 `｜免费`；顶部工具计数 176→177。
- 顺手本地化 favicon：`fetch_logos.py` 单域名抓取逻辑抓到官网高清图标 `logos/image.sunls.de.png`（43KB），并 merge 进 `logos/manifest.json`（现 139 条，未整体重写以免覆盖用户未提交改动）。
- 运行 `tests/verify_before_deploy.py`：**17/17 PASS**（tools.md 解析 177 工具 / favicon manifest 139 条均 OK）。
- 本地改动待部署（用户当时未下达「部署上线」）。

## SceneMint 部署上线

- 用户明确「部署上线」。工作区另有 276 个未提交改动（删118/改10/新148，含 ppt-01、template-showcase、deploy.yml 删除），经确认**仅推送 SceneMint 相关 3 文件**（tools.md / logos/manifest.json / logos/image.sunls.de.png），不删任何线上页面。
- `/tmp/sync_user_site.py` 已被清，重建 Git Data API 推送脚本 `/tmp/deploy_scenemint.py`：dry-run 确认仅 3 文件变更（tools.md、manifest 更新，image.sunls.de.png 新增）后正式推送；base_tree 继承线上其余文件 → 不触发删除。
- 线上新 commit `9f07c4e`；Pages workflow run `30788638667` success（head 对齐），`verify_pages_rebuilt.py` 部署后核验通过；线上 `youkappt.com/tools.md` 已含 SceneMint（curl 确认）。
- 用户工作区其余未提交改动（ppt-01/template-showcase/deploy.yml 等删除）仍未上线，待用户另行指示。

## 样机/素材站点筛选与收录（4 个）

- 用户给 11 个免费设计模板/样机站点，要求筛选值得加入的。逐站核验存活+定位：
  - 已收录（无需加）：ls.graphics/free-mockups、mockupworld.co（均在 `## 灵感/素材`）。
  - 真·推荐新增（已加）：Freebiesbug、Graphberry、GraphicsFuel、Screenzy —— 均存活、真免费、与现有样机站互补。
  - 淘汰：uikit.me（域名拍卖）、dingdangsheji.com（Cloudflare 拦截无法核实）、88sheji.cn/favorites/mockup（链接农场，与已有条目重复）。
  - 存疑未加：mockup.sitapix.com（所贴路径 404）、themockup.club（外链聚合站价值偏低，用户也未选）。
- `tools.md`：4 条加在 `## 灵感 / 素材 (INSPIRATION)`（LS Graphics 样机之后），工具计数 177→181；顶部注释同步。
- favicon 本地化：`/tmp/fetch_favicons_4.py` 抓 4 个域名图标（graphberry/graphicsfuel 走 Google s2 兜底），merge 进 `logos/manifest.json`（139→143 条，未整体重写）。
- 运行 `tests/verify_before_deploy.py`：**17/17 PASS**（181 工具 / 143 favicon 均 OK）。
- 仅本地改动，未部署（用户只定「加哪几个」，未说「部署上线」）。

## 4 个样机/素材站点部署上线

- 用户明确「部署上线」。重建 Git Data API 脚本 `/tmp/deploy_4mockup.py`（FILES=tools.md + logos/manifest.json + 4 个新 png），dry-run 确认仅 6 文件变更、不删任何线上页面后正式推送 → 线上新 commit `3677eaf59a7e85233d7007682a69cb851584c952`。
- 部署后核验 `verify_pages_rebuilt.py`：Pages run `30794017625` success，head 对齐；线上 `youkappt.com/tools.md` 已实测含 Freebiesbug/Graphberry/GraphicsFuel/Screenzy（各命中 1）。
- 备注：tools.md 与 manifest 为整体文件，推送时一并把你工作区对其的其余未提交改动也带上线（与 SceneMint 部署时同样情况），但**不删除** ppt-01/template-showcase/deploy.yml 等任何线上页面（这些删除仍只在本地未提交）。

## 从 novatools.cn 筛选收录 6 个 AI 工具

- 用户给 AI 工具聚合导航站 `https://www.novatools.cn/`，要求挑上面值得加入优卡导航的。先对照现行 181 工具剔除已收录（Meshy/Tripo/朱雀），按「AI×DESIGN×PPT」定位筛出强相关候选，经用户确认「加 6 个核心」。
- 逐一核实官网域名与存活后加入 `tools.md`（工具计数 181→187）：
  - 星流 Xingliu `https://www.xingliu.art`（在线设计，每日免费额度）
  - 椒图AI `https://www.jiaotuai.cn`（图片处理，每日免费额度）
  - Vectorizer.AI `https://vectorizer.ai`（图片处理，免费）
  - 可赞AI `https://kezign.cn`（AI PPT 生成，每日免费额度）
  - Pollo AI `https://pollo.ai`（视频生成，免费）
  - DomoAI `https://domoai.app`（视频生成，免费）
- favicon 本地化：`/tmp/fetch_6logos.py` 抓 6 域名（4 个官网直取：xingliu.art.ico/jiaotuai.cn.ico/kezign.cn.png/pollo.ai.ico；vectorizer.ai 官网+Google 均 404、domoai.app SSL EOF，改 DuckDuckGo favicon 服务兜底 vectorizer.ai.ico/domoai.app.ico），merge 进 `logos/manifest.json`（143→149 条）。
- 运行 `tests/verify_before_deploy.py`：**17/17 PASS**（187 工具 / 149 favicon 均 OK）。
- 仅本地改动，未部署（用户未下达「部署上线」）。novatools 导航站本身未加（属可选档，用户选的是 6 核心）。

## PPT术语图鉴 ·「改前 vs 改后」生图提示词（试水）

- 用户想在 ppthub 每个术语下加一对「同一内容、不同效果」的真实 PPT 单页对比案例，用生图工具生成；本步只产出**生图提示词**（用户明确「你只需要给我生图的提示词」）。
- 新建 `ppthub/docs/改前改后生图提示词.md`：试水 6 个术语（对齐 / 留白 / 视觉层级 / 主辅点三色 / 图表美化 / 表格美化），每个含「共用文案 + 改前中文提示词 + 改后中文提示词」。
- 设计要点（用户二次要求修正）：提示词**全中文**；两张图文字/数据完全相同只改视觉；**每个代码块自带完整共用提示词**（16:9、真实PPT截图、负向词内联），可独立一键复制、无需拼接；当成真实 PPT 截图生成；候选池为「设计原则/字体配色/排版布局/图形图示」约 70 个术语。
- 用户跑通「对齐」术语的 2 张生图后，确认做对，要求把真实图接进 ppthub。
- 接入实现：
  - 新建 `ppthub/images/beforeafter/`，放入 `alignment-before.png`（错落/改前）和 `alignment-after.png`（对齐/改后）。
  - 新建 `ppthub/beforeafter.js`，登记 `BEFORE_AFTER` 数据（术语 id → before/after 路径 + caption）。
  - `index.html` 引入 `beforeafter.js`；`app.js` modal 新增 `renderBeforeAfter(t)`，在「常见场景」后渲染「改前 vs 改后（真实案例）」区；紧接着保留「动手试试」SVG 交互 demo，两者并存。
  - `styles.css` 新增响应式两栏布局（桌面并排、手机堆叠）、图片 16:9 容器、改前/改后标签。
- 门禁全绿：`node --check` 全部通过；`tests/verify_ppthub.js` 12/12 PASS；`tests/demo_runtime_test.js` 182/182 OK。
- 本地截图验证：访问 `http://localhost:8044/ppthub/?term=alignment`，modal 正确显示两张真实对比图 + 动手试试 SVG 演示，无报错。
- 微调：`beforeafter.js` 的 `caption`（即「同一页内容：标题……整页有秩序」那段）原本显示在对比图下方，用户要求放到「改前 vs 改后（真实案例）」小标题正下方；已在 `app.js` renderBeforeAfter 把 `<div class="ba-caption">` 从 grid 后移到 h4 后，并给 `.ba-caption` 加 `margin-bottom` 保证与图片间距。
- 样式统一：用户发现「是什么 / 常见场景 / 改前 vs 改后」三段正文字号颜色不统一（16/15/14px + 两种颜色），已在 `styles.css` 统一为 15px / line-height 1.7 / 同种正文色（深色 `frost-glow`，浅色 `forest-ink`）。Playwright 验证三段 computedStyle 完全一致。
- 用户确认 3 组真实改前改后图：hierarchy（视觉层级）、whitespace（留白）、tri-color（主辅点三色）。图片复制到 `ppthub/images/beforeafter/` 并规范命名；`beforeafter.js` 登记 + 口语化 caption。**修正**：初版误将「让数据说话」组配成 chart-beauty，用户指出应为 whitespace，已将 `chart-beauty-*` 改名 `whitespace-*` 并改登记；`node --check` 通过；Playwright 验证三组 modal 正确渲染（hierarchy / whitespace / tri-color 均 OK，无 chart-beauty 残留）。
- 用户反馈「留白」真实案例不好（原「让数据说话+柱状图」主题，图表占版面、留白对比不纯粹）。已重写 `ppthub/docs/改前改后生图提示词.md` 第 2 节 whitespace：改用语义纯粹的「主张封面页」主题（主张句「好设计，七分留白」+ 英文副标题 + 一行小字），改前塞满贴边带底纹、改后大量留白居中克制。用户已重新生图并发回；已将新图替换进 `ppthub/images/beforeafter/whitespace-before.jpg` 和 `whitespace-after.png`，删除旧「让数据说话」组图，同步更新 `beforeafter.js` 的 caption。Playwright 验证 `?term=whitespace` 正确渲染改前/改后图 + 新 caption，无 chart-beauty 残留。改后图右下角有「即梦AI」水印，后续建议去掉。
- 用户新发来 4 张图，识别为 chart-beauty（图表美化）改前/改后 + table-beauty（表格美化）改前。已将 chart-beauty 组图接入 `ppthub/images/beforeafter/` 并在 `beforeafter.js` 登记 + 口语化 caption；`node --check` 通过；Playwright 验证 `?term=chart-beauty` 正确渲染。table-beauty 改后图待用户补发。仅本地，未部署。
