# 2026-07-30 工作记录

## ppthub「重复」术语 动手试试 改造（专属化、去撞车）
- 任务：用户要求用 ai-slides-workflow skill 思路，给 `repetition`(重复) 术语详情页的「动手试试」做有真实内容、不与其他术语撞车的完整 PPT 页面。
- 改动 1（`demos.js` CASES 库）：新增第 43 条真实案例「运营数据周报」(kicker OPS, part 43, img case4-park.jpg)，带 `mods:[6 个指标卡[数值,标签,环比]]`，场景为运营看板、与现有 42 条不撞车。
- 改动 2（`demos.js` DEMOS['repetition']）：弃用通用 `runCasePageDemo(c,'repetition')`，改为**专属完整 16:9 PPT 页面**——顶部绿条+页眉(OPS/运营看板/标题/副标题)+3×2 共 6 张指标卡网格+页脚。
  - 动手交互：`统一重复`(good) / `各不相同`(bad) 切换。good=6 卡同色块/圆角/字号/对齐(像一套系统)；bad=6 卡各用一套样式(方角/大圆角蓝/虚线居中/灰字/绿粗边/橙底条)，像几人拼的。
  - 案例内容进页面本体，顶部不再露「案例：」控件（符合自查规范 §6.2）。
- 验证：`node --check` 五文件全过 → `verify_ppthub.js` 13/13 全绿(184 术语) → `demo_runtime_test.js` 184 demo 运行时 0 报错。本地预览 `http://localhost:8031/?term=repetition`（8031 后台 http 服务）。
- 说明：ai-slides-workflow skill 引用的 AI Slides LegoKit 工具链(this环境无)，故按该 skill 端到端方法学(调研术语→设计真实完整页→真实内容)直接落到 ppthub 代码。未部署(用户未授权)。
- 备注：`repetition` 仍残留在 `CASE_PAGE_DEMOS` 中但已不再被调用，无害；如需整洁可后续移除（pickCaseForDemo 仍映射其到 CASES[4]，无影响）。
- **经验｜本地预览服务器必须用 run_in_background 持久化**：`(node ... &)` 脱离式后台在 Bash 命令结束后会被沙箱回收（端口随即无监听，用户再访问即打不开）。正确做法是用 Bash 工具的 `run_in_background=true` 启动长驻 http 服务（本次 task_id iMStRW，端口 8031）。ppthub 图片在 `images/` 子目录，页面用 `./images/<file>` 引用，curl 直接打 `/<file>` 会 404 属误测。
- **教训｜repetition「动手试试」必须用 960×540 SVG 统一范式**：初版误用纯 HTML/CSS 卡片（div+CSS grid）写完整页面，违反项目统一规范——其他设计原则 demo（alignment/proximity/contrast…）全走 `buildCasePageSvg` 的 `<svg viewBox="0 0 960 540" width="100%" height="100%">` + `.mini-slide`（`width:100%;aspect-ratio:16/9`）容器，移动端靠 SVG viewBox 自动按比例缩放。已改正：新增 `buildRepetitionSvg(cs,mode)`（960×540 SVG，3×2 共 6 卡看板，good=统一绿边/bad=各不相同 6 套样式），`DEMOS['repetition']` 用 `.mini-slide` 容器 + good/bad 按钮调用之。所有「动手试试」demo 一律 SVG 化，禁止用 HTML/CSS 硬写页面（固定 px 字号小屏会挤/溢出，破坏按比例缩放）。

## 安装 oma-slide skill + 用其方法论重做「重复」demo（对齐常见场景）
- 用户要求：安装开源 skill `oma-slide`（github first-fluke/oh-my-agent），并用它给 `repetition`「动手试试」生成完整 PPT 页面案例，且**保留 960×540 SVG+移动端缩放范式**、**内容与「常见场景」保持一致**。
- 沙箱网络：github.com 被 502 拦截（git/curl 均失败），但 WebFetch 走工具出口能取 raw 文件。npm 包 `@oh-my-agent/oma`（oma slide CLI）404 不存在 → CLI 无法在本沙箱运行，故"用 skill 生成 PPT 的能力"= 应用其 **design doctrine / 12 风格预设 / fixed-stage 规范** 来撰写 demo，而非真跑 CLI。
- 安装：已把 SKILL.md + resources/{design-doctrine.md, style-presets.md, fixed-stage.md} 写入 `~/.workbuddy/skills/oma-slide/`（满足"安装"）。oma-slide 的 PPTX 导出目标正是 **960×540**（LAYOUT_WIDE 13.333×7.5in = 960×540pt），与 ppthub 的 SVG 舞台模型天然对齐。
- pivot demo 内容对齐 常见场景：`repetition.scenario`（data.js 176）=「每个章节封面都套同一种配色和排版模板→整份演示节奏统一」。故把 demo 从"6 卡看板"改为**三章封面墙**：
  - good(统一重复)= 三章封面(01 品牌诊断 / 02 视觉规范 / 03 应用落地)共用同一套 ink-press 模板（新闻纸底 `#f5f0e8` + 朱红 `#d4380d` 左条 + 衬线大数字 + 同版式）→ 节奏统一。
  - bad(各不相同)= 三章各一套版式（橙居中无左条 / 蓝右上数字 / 绿底部数字）→ 像三份 deck，杂乱。
  - 设计遵循 oma-slide doctrine：committed palette（避开紫蓝渐变cliché）、distinctive 衬线数字（Georgia，非禁用默认字体）、8px 栅格、z 层序、WCAG AA 对比。
- 案例库：`CASES[42]` 改为 `id:'brand-vi'`「品牌 VI 升级提案」(kicker BRAND, partName 章节封面)，内容为真实三章结构，与 42 条旧案例不撞车；`DEMOS['repetition']` 改用 `CASES.find(x=>x.id==='brand-vi')`，hint 文案同步改为"三章同模板/三章各一套"。
- BUG：`buildRepetitionSvg` 初版把 `const FB` 定义在 `cover()` 内部，但 `return` 在外部作用域引用 → 运行时 `FB is not defined`（runtime test 抓到 [THROW] repetition）。已把 `FB` 提到函数外层作用域修复。
- 验证：node --check 五文件过 → verify_ppthub.js 13/13(184) → demo_runtime_test.js 184 demo **0 报错**。预览 `http://localhost:8031/?term=repetition`（8031 后台服务仍在线）。未部署（用户未授权）。
- **用户确认制作策略**：认可「借外部 skill 方法论手写（参考其美学原则）」的做法，要求固化进本项目案例制作流程。已写入 `ppthub/docs/自查规范.md`：① §6.2 强化「统一 SVG 范式」（960×540 SVG + `.mini-slide`，禁纯 HTML/CSS 写完整页，参考 `buildRepetitionSvg`）；② 新增 §6.4「制作策略：借方法论手写」（背景/批准做法 5 条/可用参考/透明红线）。同步精简超限的 MEMORY.md 并补入该约定。
- **补强 §6.4**：用户追问「新约定里有没有『有真实内容的完整PPT页面案例（不与其他术语重复）+ 与常见场景一致』这条」——原 §6.4 把它拆散在多处未集中。已在 §6.4 顶部加「🎯 制作目标（硬性）」四要素（真实内容 / 完整页面 / 不撞车 / 与 scenario 一致），与用户原话逐字对齐。

## 按 §6.4 新约定重做【衬线 / 无衬线】serif-sans 动手试试
- 用户指令：给 `serif-sans`(衬线/无衬线) 按新约定做「有真实内容的完整PPT页面案例（不与其他术语重复）+ 与常见场景一致」。
- 旧实现（demos.js 984 行）：通用占位——只把 CASES 标题当大字 toggle serif/sans，带「案例：」标签+「↻ 换案例」按钮（违反 §6.2），且非完整页。已整体替换。
- 新增唯一 CASES：`brand-annual`「城市绿洲品牌年报」(kicker TYPE, img case3-book.jpg, mods=[12座/38万/96%])，与 repetition 的 `brand-vi` 不撞车。
- 新增 `buildSerifSansSvg(cs,mode)`：960×540 SVG 完整品牌页，committed palette（暖纸 `#f7f3ea`+陶土 `#b5532a`，避紫蓝 cliché）、explicit 衬线/无衬线字体栈（不靠 var，保证对比真显）、8px 栅格、z 层序。
  - 对齐 scenario（文化/品牌用衬线显气质、数据用无衬线显利落）：good=统一配对（标题/金句衬线 + 数据/正文无衬线，一套规则）；bad=随机混搭（标题无衬线、正文衬线、数字衬线…每处各来一套，无系统）。
- `DEMOS['serif-sans']` 改为 `.mini-slide` 缩放容器 + good/bad 按钮（无案例控件），hint 解释配对逻辑。
- 验证：node --check 五文件过 → verify_ppthub.js 13/13(184) → demo_runtime_test.js 184 demo **0 报错**。预览 `http://localhost:8031/?term=serif-sans`（8031 后台在线）。未部署（用户未授权）。

## 按 §6.4 批量重做下一批 10 个术语「动手试试」（共享 SVG 框架）
- 用户指令：继续做下一批 10 个术语（设计原则+版式布局核心簇），全部按 §6.4 四要素（真实内容 / 完整 960×540 SVG 页 / 不撞车 / 与 scenario 一致）重做。
- 选词（仍用通用 runCasePageDemo 的 10 个）：`alignment` `proximity` `contrast` `hierarchy` `whitespace` `focus` `grid` `golden-ratio` `rule-of-thirds` `f-pattern`。
- 新增共享 helper（`demos.js`，runCasePageDemo 前）：`demoPage({cs,mode,accent,bg,good,bad,tagGood,tagBad})` 统一画 960×540 页眉页脚+内容区（x48..912 / y162..498）；`svgTxt()` / `svgRect()` 图元助手；常量 `SVG_SANS/SVG_SERIF/SVG_MONO/SVG_INK/SVG_MUTED`（显式字体栈，不靠 var，规避 SVG 内 var() 失效）。各术语只需提供 good/bad 场景函数 → 大幅降体积、统一范式。
- 新增 10 条唯一 CASES（part 45..54，id 各自唯一）：align-launch / prox-white / cont-fund / hier-retro / ws-poster / focus-kpi / grid-matrix / gold-cover / rot-portrait / fp-news，内容对齐各自 scenario，contrast/focus 带 `mods` 真实指标。
- 10 个 `DEMOS` 由 `runCasePageDemo` 通用占位改为基于 `demoPage` 的专属完整页，good/bad 对比扣住 scenario：对齐(统一左参考线 vs 各起边) / 亲密性(分组靠拢 vs 散开) / 对比(单数字高亮 vs 全一样大) / 层级(结论置顶 vs 平铺) / 留白(大留白 vs 塞满) / 焦点(目标卡高亮 vs 全灰) / 栅格(3列等宽 vs 宽窄不一) / 黄金分割(0.618:0.382 vs 五五开) / 三分法(人物偏右交点 vs 居中) / F型(标题置顶+左上导语 vs 居中堆叠)。
- 验证：node --check 五文件过 → verify_ppthub.js **13/13(184)**（SVG 裸标签配对升至 26 对）→ demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8031/?term=alignment` 等（8031 后台在线）。未部署（用户未授权）。
- **复用提示**：后续再做术语时，直接调 `demoPage` + 加一条唯一 CASES 即可，无需每个术语再写一整套 buildXxxSvg；repetition/serif-sans 仍各自独立 build 函数（历史产物，未强改）。

## 按 §6.4 批量重做截图中 7 个术语「动手试试」
- 用户指令：按最新规范重做截图中 7 个术语的「动手试试」案例（减法设计/品牌规范/VI/视觉流/信息密度/视觉一致性/统一/版面率/图版率/格式塔原理）。
- 识别 id：`less-is-more` `brand-vi` `visual-flow` `info-density` `consistency` `fill-rate` `gestalt`；旧实现均为通用 `runCasePageDemo` 占位，已整体替换。
- 新增 7 条唯一 CASES（part 55..61）：`less-slide`(产品核心卖点页) / `brand-guidebook`(企业品牌手册页) / `flow-report`(市场调研洞察页) / `density-brief`(城市数据简报页) / `consistent-deck`(产品功能介绍页) / `fill-cover`(品牌发布会封面) / `gestalt-list`(功能模块说明页)，均带真实内容且不撞车。
- 新增 7 个专属 builder（960×540 SVG，位于 buildSerifSansSvg 之后），各自对齐 scenario：
  - `less-is-more`：good=一页一事（大数字+金句+留白），bad=5 卖点小字挤满。
  - `brand-vi`：good=三页缩略统一 Logo/标题/页脚，bad=三页各一套版式。
  - `visual-flow`：good=结论置顶+编号路径下沉，bad=元素平均分散无路径。
  - `info-density`：good=6 指标卡片分组留白，bad=卡片过密无呼吸。
  - `consistency`：good=4 卡片统一图标/色彩/圆角，bad=3 种风格混杂。
  - `fill-rate`：good=封面低版面率（一句话+大量留白），bad=塞满装饰按钮。
  - `gestalt`：good=模块内紧凑+模块间大留白，bad=元素均匀散乱。
- 兼容修复：新增 CASES 中部分条目无 `tw` 字段，导致依赖 `Math.random()*CASES.length` 的 `table-beauty`/`excel-table` demo 偶发 `Cannot read properties of undefined (reading 'aPts')`。已将两个 demo 改为先 `const twCases = CASES.filter(x => x.tw && x.tw.aPts && x.tw.bPts)`，再从 `twCases` 中随机取案例，避免随机到无 tw 的新增 CASES。
- 验证：node --check 五文件过 → verify_ppthub.js **13/13(184)**（SVG 裸标签配对升至 33 对）→ demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8031/?term=less-is-more` 等（8031 后台在线）。未部署（用户未授权）。

## 修正【层级 hierarchy】demo 头重脚轻 + 修复 combo-chart 运行时兼容
- 用户反馈：hierarchy「动手试试」红框头部（REVIEW/信息层级/项目复盘首页/副标题）占用区域过大，像套固定模板硬塞，导致正文内容区小、案例看不到重点。
- 根因：`DEMOS['hierarchy']` 原用共享 `demoPage` 框架，顶部默认画 kicker/partName/title/subtitle 四行（y36~132），对纯文字层级对比页确实头重脚轻。
- 修正：新增专属 `buildHierarchySvg(cs, mode)`，头部压缩到只有一行 kicker+partName 与页面名（y40~74），正文区从 y110 放到 y470，让「结论置顶 vs 平铺无层级」成为视觉中心。
  - good：左侧 accent 竖条 + 46px 大结论标题 + 分隔线 + 18px 三行支撑小字，层次清晰。
  - bad：四行 18px 同样大小颜色平铺，无轻重。
- `DEMOS['hierarchy']` 改为调用 `buildHierarchySvg`，不再走 `demoPage`。
- 兼容修复：新增 CASES 中部分无 `tw`，`combo-chart` 也随机到无 tw 案例触发 `aPts undefined`。已统一改为 `TW_CASES = CASES.filter(x => x.tw && x.tw.aPts && x.tw.bPts)` 后只从 `TW_CASES` 随机取。
- 验证：node --check 五文件过 → verify_ppthub.js **13/13(184)**（SVG 裸标签配对升至 34 对）→ demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8031/?term=hierarchy`（8031 后台在线）。未部署（用户未授权）。

## 泛化「压缩页眉」到 13 个术语（对齐/亲密性/对比/留白/焦点/栅格/减法/品牌VI/视觉流/信息密度/一致性/版面率/格式塔）
- 用户反馈：上述 13 个术语的「动手试试」与 hierarchy 同样头重脚轻——共享 `demoPage` 四行页眉(kicker/partName/title/subtitle @y36~132)占太多垂直空间，正文内容区小、案例看不到重点。
- 泛化修正：把 hierarchy 的压缩页眉思路提炼为共享 `demoPageCompact(o)`（位于 `demoPage` 之后）：两行页眉（kicker·partName @y40，标题 @y74）+ 分隔线 @y92，内容区从 y110 放到 y498（比旧 `demoPage` 多约 110px 高度），页脚 y520。复用 svgTxt/svgRect/SVG_* 常量。
- 13 个术语全部改用 `demoPageCompact` 并把 good/bad 场景放大到新内容区（大字号标题、大卡片、大数字，重点更突出）：
  - 6 个原 `demoPage` 术语（alignment/proximity/contrast/whitespace/focus/grid）：`demoPage`→`demoPageCompact`，场景坐标整体下移放大。
  - 7 个专属 builder（less-is-more/brand-vi/visual-flow/info-density/consistency/fill-rate/gestalt）：其逻辑内联进各自 `DEMOS` 条目的 good/bad 场景函数，**删除** 7 个废弃 `buildXxxSvg`（死代码，已无引用）。
- 顺手修复 3 个随机案例 demo 的 `cs.tw` 未守卫崩溃（使 `demo_runtime_test` 稳定 0 报错）：
  - `chart-elements`：`cs.tw.a` → `((cs.tw && cs.tw.a) || '系列')`。
  - `data-viz`：随机取案例改为只从 `twCases`(带 tw.aPts/bPts) 池取。
  - `layout` 的 twocol 分支：`cs.tw` 缺失时回退到 `cs.points` 折两栏。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错（连跑 5 次随机均 0）**。预览 `http://localhost:8055/?term=alignment`（8055 后台在线）。未部署（用户未授权）。
- **约定沉淀**：今后新增/重做「动手试试」一律用 `demoPageCompact`（禁止再用旧四行头重的 `demoPage`）；所有随机取案例的 demo 凡用 `cs.tw.*` 必须先 `filter` 到带 tw 的池或加 `cs.tw &&` 守卫。

## 修正【焦点引导 focus】bad 案例命名
- 用户反馈：focus「动手试试」的 bad 按钮叫「全部一个样」不对，这更像是「一致性/统一」术语的反例，而不是「焦点引导」的反例。
- 修正：`ppthub/demos.js` `DEMOS['focus']`：
  - bad 按钮文案从「全部一个样」改为「视线无主」；
  - tagBad 从「全部一个样」改为「视线无主」；
  - hint 改为「视线无主：三张卡大小、颜色、位置权重相同，都在抢注意力，读者不知道先看哪个指标。」
- 视觉保持三卡等权重（准确表现无焦点引导），但命名和解释回到 `focus` 本身。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8055/?term=focus`。未部署（用户未授权）。

## 重做【格式塔原理 gestalt】动手试试案例
- 用户反馈：原 bad 案例把 3 个模块重复排成 3×3 均匀网格，内容虚假、不像真实 PPT，且 good 与 bad 数量不一致（3 vs 9），对比不公平。
- 修正：`ppthub/demos.js`：
  - `CASES['gestalt-list']` 改为「产品能力矩阵」：6 项能力分用户端/管理端两组，subtitle/points/body 同步更新。
  - `DEMOS['gestalt']` 全新 good/bad 场景：
    - good（相近成组）：用户端 3 卡在上、管理端 3 卡在下，各组加浅色背景底+组标题，组间大留白，结构一目了然。
    - bad（无分组）：同样的 6 项能力平均排成 2 行×3 列，无组标题、无背景、无额外间距，像散装列表。
  - bad 按钮文案从「均匀散乱」改为「无分组」，hint 同步更新。
- 关键改进：① 内容真实（6 个不同模块）；② good/bad 同内容同数量，仅分组方式不同，对比公平；③ 直接扣住格式塔「相近律 / 共同区域」原理。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8055/?term=gestalt`。未部署（用户未授权）。

## 修正 gestalt good 案例「两组不对称 / 组内规范不一致」
- 用户反馈：上一版 gestalt good 的两个组底高度不同（210 vs 130），且组内标题/卡片相对位置没对齐，不够规整；并反问正面案例是否参考美学原则。
- 修正：`DEMOS['gestalt']` good 重写为**两组完全对称、排版规范 100% 一致**：
  - 两组组底均为 y/h=118/170 与 326/170（等高）；组标题相对组顶均 +24；卡片相对组顶均 +54、高均 100、底部留白均 16 → 组内结构逐像素对齐。
  - 两组卡片内部样式完全一致（白底、圆角10、图标品牌绿、标题16粗黑、说明13灰）；只用**绿 / 蓝灰两阵营色 + 组标题**区分用户端/管理端，把分组交给格式塔「相似性」自动归类。
  - bad 改为 6 项能力**打乱混排**（order=[2,4,0,5,1,3]），无组标题/背景，进一步凸显"无分组=认不出结构"。
- 设计原则应用：oma-slide committed palette（固定绿+克制的蓝灰，避渐变）、8px 栅格、WCAG 层次；格式塔相近律/相似律本身是案例核心。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8055/?term=gestalt`。未部署（用户未授权）。

## 修复 consistency 居中 + 重做 fill-rate/info-density 反面案例
- 用户反馈（三图）：
  1. fill-rate 反面案例不好，要换一个；
  2. consistency 正面案例 4 个区块没有居中对齐；
  3. info-density 反面案例不好，要换一个。
- 修正：
  - **consistency**：good/bad 的 4 张卡片 x 坐标从 `[70,300,530,760]` 改为 `[35,265,495,725]`。4×200+3×30=890，居中边距=(960−890)/2=35，现在左右留白相等、真正居中。
  - **fill-rate 反面**：从原来的「4 个信息块+2 按钮」升级为更典型的高版面率反面：顶部绿色横幅、标题+副标题、4 个信息卡、3 个 CTA 按钮、底部主办/合作媒体栏——整页几乎被塞满，彻底失去封面应有的高级感和呼吸感。
  - **info-density 反面**：从「6 卡紧密排列」升级为「信息过载」：同样 6 个指标，但每张卡额外塞入环比箭头、分隔线、迷你进度条，顶部加总览标题、底部加数据来源注脚——元素过多、无主次、无留白，读者抓不住重点。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错**。预览 `http://localhost:8055/?term=consistency` / `?term=fill-rate` / `?term=info-density`。未部署（用户未授权）。

## 按 7 张截图逐个修正「动手试试」视觉/案例
- 用户反馈（7 张截图对应 7 个术语）：
  1. `gradient` 默认状态是同一绿色，不像真实渐变；
  2. `line-height` 只有滑块，缺少真实文字段落；
  3. `wordart` 艺术字/变形案例太俗气，要重做；
  4. `analogous` 邻近色案例四条全绿，没体现蓝→蓝绿→绿；
  5. `muted-color` 只有四个小方块，没有真实 PPT 页面对比；
  6. `color-wheel` 色块内文字多余，要去掉；
  7. `font-pairing` 页面不居中、排版不好看，要重做成完整页面。
- 修正（`ppthub/demos.js`）：
  - **gradient**：两个 color input 默认值改为 `#2f6f3a` → `#3a7bd5`，默认方向改为 135° 对角，按钮增加「对角/纵向/横向」，.mini-slide 展示完整渐变封面（不再塞文字）。
  - **line-height**：.mini-slide 改为真实 PPT 内页样式（标题+分隔线+正文段落），滑块控制正文行距，内容取自 `cs.body + points`。
  - **wordart**：三种状态重做——普通标题（干净大标题+副标题）、质感艺术字（135° 绿蓝渐变填充+柔和阴影）、节奏变形（单字上下错落+字间距），均配副标题，更像真实封面。
  - **analogous**：改为真实 PPT 页面，顶部标题+分隔线，下方三栏卡片分别用蓝 `#3a7bd5` / 蓝绿 `#2a9d8f` / 绿 `#2f6f3a`，对应战略目标/关键动作/预期结果，体现色环相邻的和谐感。
  - **muted-color**：改为 good/bad 切换的真实 PPT 封面：good=莫兰迪灰低饱和（标题/副标题/胶囊标签均用灰调绿），bad=高饱和红/蓝/青标签，直观对比「显贵 vs 刺眼」。
  - **color-wheel**：去掉色块内的案例标题文字，色块只展示颜色，保留旁边色值 `#2f6f3a` 和底部说明。
  - **font-pairing**：重做成 **960×540 SVG 完整页面**，三种字体搭配在居中、对齐的真实页面中呈现：无衬线标题+衬线正文、同族字重标题/正文、中英混排（中文标题+英文斜体副标+无衬线正文），解决旧版不居中问题。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错（连跑 3 次）**。预览 `http://localhost:8055/?term=gradient` / `line-height` / `wordart` / `analogous` / `muted-color` / `color-wheel` / `font-pairing`。未部署（用户未授权）。

## 按 5 张截图重做「动手试试」案例（字体授权/字体选择/竖排文字/主题色/色彩心理学）
- 用户反馈（5 张截图对应 5 个术语）：`font-license`（字体授权列表不像案例）、`replace-font`（只换标题字无页面）、`vertical-text`（只有两个字无场景）、`theme-palette`（紫/青都映射成绿色，且只是小 slide 色条）、`color-psychology`（只有色圈+文字）。
- 改动：
  - 扩展 `demoPageCompact` 支持 `o.tag` 动态标签，用于多状态 demo（不止 good/bad）。
  - 新增 5 条唯一 CASES（part 62..66）：`font-license`（餐饮品牌开店计划）/`replace-font`（城市绿洲品牌年报）/`vertical-text`（供应链降本专项）/`theme-palette`（产品白皮书内页）/`color-psychology`（产品白皮书内页），内容与各自 scenario 对齐。
  - **font-license**：改为 good/bad 完整页——good=全部免费商用字体（思源/站酷），bad=混入需授权字体（微软雅黑/方正），列表带 ✓/! 状态与风险提示。
  - **replace-font**：4 种字体（黑体/宋体/楷体/等宽）切换时改变整个年报页标题+数据卡气质，每种配不同主色和说明。
  - **vertical-text**：横排=现代商务封面（标题左对齐+副标题+要点）；竖排=国风封面（标题从右至左竖排+左侧落款+分隔线）。
  - **theme-palette**：紫/青/橙三套真实主题色，一键改变标题区、数据卡、强调线、CTA 按钮，展示主题色驱动整套页面。
  - **color-psychology**：红/蓝/绿/黄四种颜色分别渲染四个真实场景页面（促销紧迫感/金融信任感/环保自然感/提醒活力感）。
- 验证：node --check 过 → verify_ppthub.js **13/13(184)** → demo_runtime_test.js **184 demo 0 报错（连跑 4 次）**。预览 `http://localhost:8055/?term=font-license` / `replace-font` / `vertical-text` / `theme-palette` / `color-psychology`。未部署（用户未授权）。

## 删除术语 text-link（文本框链接 / 文字溢出）
- 用户要求删除该术语。6 处引用全清：data.js 术语对象、terms-extra.js 扩展、demos.js 元信息+`'text-link'(c)` 函数入口、`case 'textlink'`（保留 `case 'column'` 共享分支）、beforeafter.js `BA_VISUALS['text-link']`。
- 文档同步：`AGENTS.md` / `docs/自查规范.md` 写死的「184」→「183」（AGENTS.md 第 30 行剩余待补 126→125）。
- **注意**：`autofit`（文本自动适配，bad=文字溢出）是不同概念，保留不动。
- 验证：4 文件 node --check 全过 → verify_ppthub.js **13/13(183)** → demo_runtime_test.js **183 demo 0 报错（连跑 3 次）**。未部署（用户未授权）。

## 按 12 张截图重做「动手试试」案例（对角线/对称/视觉平衡/卡片式/栏宽/F型/三分法/自动适配/段间距/内边距/页边距/中心对称）
- 用户反馈（12 张截图）：`diagonal-flow` `symmetry` `visual-balance` `card-layout` `column` `f-pattern` `rule-of-thirds` `autofit` `para-spacing` `text-margin` `margin` `center-sym` 仍用共享 `runCasePageDemo` 或旧 `demoPage`，存在头重脚轻、案例通用、重点不突出等问题，要求全部重做。
- 改动（`ppthub/demos.js`）：
  - 12 个术语全部从 `runCasePageDemo` / `demoPage` 改为 `demoPageCompact` 自定义 SVG（2 行页眉 + 内容区 y110–498 + 页脚），共用 `svgTxt/svgRect/SVG_*` 常量。
  - 每个术语绑定真实 CASES（按 title 或 id 稳定选取），good/bad 场景紧扣各自 scenario：
    - `diagonal-flow`：标题左上+对角线箭头+配图右下 vs 水平横排。
    - `symmetry`：中轴左右等重 vs 右侧过重左侧空洞。
    - `visual-balance`：左文+小色块 vs 右大图下方太空。
    - `card-layout`：3 张横向对齐卡片 vs 纯文字列表。
    - `column`：双栏约 1/2 页宽 vs 单行铺满。
    - `f-pattern`：通栏标题+左上导语+F 型路径高亮 vs 居中堆叠。
    - `rule-of-thirds`：人物落右三分交点 vs 死板居中。
    - `autofit`：文本框自动缩小/换行完整保留 vs 文字溢出框外。
    - `para-spacing`：三段带段前段后间距 vs 段落粘连成一块。
    - `text-margin`：文字离框边 16px 舒适 vs 贴边文字。
    - `margin`：四周 32px 安全边距 vs 内容顶到边界。
    - `center-sym`：标题/主图/要点沿中轴对称 vs 重心偏右左侧空洞。
- 验证：`node --check` 过 → verify_ppthub.js **13/13(183)** → demo_runtime_test.js **183 demo 0 报错**。本地预览 `http://localhost:8055/?term=diagonal-flow` 等（8055 后台服务仍在线）。未部署（用户未授权）。

## 继续修正 6 个术语「动手试试」（去重复模板、固定版式换字体、删除按钮放大正文）
- 用户反馈（6 张截图）：
  1. `font-pairing` / `font-license` / `vertical-text` / `color-psychology`：案例重复、被固定模板约束，要重做；
  2. `replace-font`：之前版本视觉上像改颜色而非换字体，要固定版式/颜色只换字体；
  3. `theme-palette`：直接把按钮删掉、放大正文内容。
- 改动（`ppthub/demos.js`）：
  - **font-pairing**：三种搭配对应三种真实版式——`无衬线+衬线`=居中封面（无衬线大标题+衬线正文+底部要点）；`同族字重`=数据页（同族字体靠字重分层：标题 800 / 数据 800 / 说明 400）；`中英混排`=国际化方案页（左中文标题+英文斜体副标｜右英文正文+编号要点）。
  - **font-license**：good/bad 共用同一页版式，good=思源宋体/黑体/站酷快乐体全部 ✓；bad=混入微软雅黑/方正兰亭黑并用红色 ! 警示。
  - **vertical-text**：横排=现代商务封面（左对齐大标题+副标题+CTA 按钮）；竖排=国风专项页（右侧竖排标题+左侧落款+分隔线+印章），两页内容不同。
  - **replace-font**：完全固定版式和绿色系，只切换标题/数据/说明的字体栈；宋体加书卷气横线、楷体加印章红点、等宽加 `// font-family: monospace` 注释，强化不同字体的场景气质。
  - **color-psychology**：四种颜色四种不同结构——红=促销卡片（大价格+CTA）、蓝=金融年报（数据+柱状分布）、绿=环保封面（曲线+大数字+CTA）、黄=系统通知弹窗（感叹号+时长+按钮）。
  - **theme-palette**：删除所有切换按钮，直接并排展示三套主题色（紫/青/橙）驱动的同一页面结构，每套 260×380 大卡片，标题/数据/按钮/强调线全部联动。
- 验证：`node --check` 过 → verify_ppthub.js **13/13(183)** → demo_runtime_test.js **183 demo 0 报错**。本地预览 `http://localhost:8055/?term=font-pairing` 等（8055 后台服务仍在线）。未部署（用户未授权）。

## 删除全部术语的「改前 vs 改后」模块
- 用户要求：删除所有术语详情页中的「改前 vs 改后」区块。
- 改动：
  - `ppthub/app.js`：删除弹窗内 `t.beforeafter` 渲染逻辑。
  - `ppthub/index.html`：删除 `<script src="beforeafter.js"></script>` 引用。
  - `ppthub/beforeafter.js`：删除整个文件。
  - `ppthub/terms-extra.js`：删除全部 183 条术语的 `beforeafter` 字段（含同行内联形式）。
  - `ppthub/styles.css`：删除 `.modal-ba` / `.ba-grid` / `.ba-panel` / `.ba-tag` / `.ba-slide` / `@keyframes baPulse` 等已失效样式。
  - `ppthub/tests/verify_ppthub.js`：移除 BC 常量检查项，重新编号为 12 项；不再读取 beforeafter.js。
  - `ppthub/tests/demo_runtime_test.js`：移除 `window.eval(read('beforeafter.js'))`。
  - `ppthub/AGENTS.md`：移除 beforeafter.js 描述、改前改后区块说明、相关铁律；更新门禁命令与项数（13→12）。
  - `ppthub/docs/自查规范.md`：移除适用范围中的 beforeafter.js、语法检查命令中的 beforeafter.js、13 项列表中的 BC 检查、新增术语工序中的 beforeafter?、检查清单项数（13→12）；删除整个 §7「改前改后 BA_VISUALS 制作范式」。
- 验证：`node --check` 四文件全过 → `verify_ppthub.js` **12/12 PASS（183 术语）** → `demo_runtime_test.js` **183 demo 0 报错**。本地预览 `http://localhost:8055/ppthub/`（8055 后台服务仍在线）。未部署（用户未授权）。

## 删除术语 map-chart（地图图表）
- 用户要求删除「地图图表」术语。4 处引用全清：data.js 术语对象（数组末元素，无尾逗号）、terms-extra.js 扩展（末块，删后前一 ink-math 去逗号）、demos.js `'map-chart'(c)` 函数（注意结尾 `render(btn.dataset.r))` 双层括号，正则须 `\)\)`）。
- 文档同步：`AGENTS.md` / `docs/自查规范.md` 的「183」→「182」（个 PPT 术语 / 条 / 已 183 个全部完成 / ≈183 术语对应）。
- grep 确认全站无 `map-chart` / `地图图表` 残留引用。
- 验证：`node --check` 四文件全过 → `verify_ppthub.js` **12/12 PASS（182 术语）** → `demo_runtime_test.js` **182 demo 0 报错**。未部署（用户未授权）。

## 优卡导航新增工具：千问办公（QwenWork）
- 用户要求「千问办公 加入 优卡导航」。在 `tools.md` 的 `## AI 助手 (ASSISTANT)` 分类（豆包/通义千问/文心一言之后）新增条目：`- [千问办公](https://qwenwork.cn/) — 阿里一站式 AI Agent 办公工具，一句话交付 PPT/Word/Excel/网页/代码，深度接入钉钉。｜每日免费额度`。
- 顶部计数注释 173→174；grep 确认 tools.md 实际条目数=174。
- 图标本地化：从 icon.horse 抓取 qwenwork.cn favicon（WebP）→ sips 转 180×180 PNG → `logos/qwenwork.cn.png`；并在 `logos/manifest.json` 登记 `"qwenwork.cn":"qwenwork.cn.png"`（现 136 条）。缺失则 index.html 自动色块首字兜底（已确认安全）。
- 付费状态判定：Freemium（免费基础体验 + 按月订阅），与同分类 AI 助手一致标 `每日免费额度`。
- 预览：`python3 -m http.server 8123`（后台 task GsP7GL）已起；curl 验证 tools.md 含千问办公、logo png 200、index 200。未部署（用户未授权）。

## 优卡导航新增工具：WorkBuddy（腾讯 AI 办公智能体）
- 用户拼写 `workboddy` 实为 **WorkBuddy**（腾讯 AI 办公智能体，与千问办公同类）。已加入 `tools.md` 的 `## AI 助手 (ASSISTANT)` 分类（千问办公之后）：`- [WorkBuddy](https://workbuddy365.com.cn/) — 腾讯 AI 办公智能体，一句话下达任务，自主交付文档/表格/PPT，支持手机远程遥控。｜每日免费额度`。
- 顶部计数 174→175；实际条目数=175。
- 图标本地化：icon.horse 抓 `workbuddy365.com.cn` favicon（直接 PNG 256×256）→ `logos/workbuddy365.com.cn.png`；manifest 登记 `"workbuddy365.com.cn":"workbuddy365.com.cn.png"`（现 137 条）。
- 付费状态：Freemium（基础功能免费 + 订阅），与同分类一致标 `每日免费额度`。
- 验证：curl 确认 tools.md 第98行 WorkBuddy、计数175、logo png 200。未部署（用户未授权）。

## 🚀 部署上线（用户亲口「部署上线」授权）
- 流程：`tests/verify_before_deploy.py`（**17/17 PASS, EXIT=0**，tools.md 解析 175 工具、ppthub 编码/SVG/txt 全 OK）→ `/tmp/sync_user_site.py`(EXECUTE=1) Git Data API **单 commit `4661b71f`**（21 PUT + 1 DEL，`ppthub/beforeafter.js` 删除）→ `tests/verify_pages_rebuilt.py`：**Pages workflow run 30519425003 success，head==main HEAD**，EXIT=0。
- 线上实测：主站 tools.md 含「千问办公」「WorkBuddy」、计数「共 175 个工具」(175行)、2 新 logo 均 200；ppthub data.js 无 map-chart(0)、beforeafter.js→404、terms=182。全部生效。
- 注意：dry-run 曾确认本地 ppt-01/、template-showcase/ 等仍在磁盘（仅 git 索引旧 staged），sync 未误删线上任何内容（仅 1 个 DEL=beforeafter.js）。

## 🐞 mockup 子页 .notice 提示条「文字看不清」bug 修复
- 症状：右上角成功/信息提示条（`.notice`）文字几乎看不见。根因：`.notice` 文字 `color:var(--mist)`（--mist=#565c47 深橄榄绿）落在 `background:rgba(26,51,0,.94)`（深绿 #1a3300）上，对比度极低。另 `.notice.info svg{color:var(--frost)}`（--frost=#1a3300）与背景同色，info 图标也不可见。
- 修复（`mockup/assets/index-d560a517.css`，构建产物直改）：`.notice` 文字 `var(--mist)`→`var(--midnight)`(#fcfaf5 近白) + `font-size:12px`→`13px` + `font-weight:500`；`.notice.info svg` `var(--frost)`→`var(--midnight)`。替换均唯一命中并校验。对比度 >10:1。
- 验证：本地 `python -m http.server 8137`（mockup 目录，后台 task YZX2qr）已起；curl 确认 CSS 含新规则、index 200。未部署（本轮仅为 bug 修复，未授权上线，等用户下令）。

## 🐞 mockup 子页「张数」下拉菜单文字看不清 bug 修复
- 症状：截图显示选择「11 张」的下拉菜单展开后，深绿色背景上文字几乎看不见。根因：`.count-menu` 背景为 `rgba(26,51,0,.98)`（深绿 #1a3300 近似），而 `.count-option` 文字 `color:var(--mist)`（--mist=#565c47 深橄榄绿），深底深字对比度极低。
- 修复（`mockup/assets/index-d560a517.css`，构建产物直改，替换均唯一命中）：
  - `.count-menu` 背景 `rgba(26,51,0,.98)`→`rgba(252,250,245,.98)`（浅色），阴影与边框同步调柔和；
  - `.count-option` 文字 `var(--mist)`→`var(--frost)`(#1a3300 深绿)，`font-size:11px`→`12px` 并加 `font-weight:500`；
  - `.count-option:hover` / `.count-option.selected` 文字 `var(--pure)`(白)→`var(--frost)`，避免浅色底白字不可见；
  - `.count-menu::-webkit-scrollbar-thumb` `var(--edge)`→`var(--mist)`，保证滚动条在浅色底可见。
- 验证：本地 `http://localhost:8137/` 已起；curl 确认 CSS 已下发新规则；index 200。
- **已部署上线**（用户授权「部署上线」）：`verify_before_deploy.py` **17/17 PASS** → `/tmp/sync_user_site.py`(EXECUTE=1) 单 commit `2d5e83ee`（1 PUT，仅 mockup CSS）→ `verify_pages_rebuilt.py`：Pages workflow run `30526919813` success，head==main HEAD。线上 curl 确认 `.count-menu` 背景已浅色、`.count-option` 文字已深绿 12px 500、hover/selected 均深绿、滚动条 mist。全部生效。
