# 2026-07-28 工作日志

### 07:52 — ppt-tips 4 类 demo 共性问题修复
- **格式刷 formatBrush**：「改前」区域 `rgba(255,255,255,.25)` 在白底上不可见 → 改为 `rgba(47,111,58,.25)` 可见绿；提示文字字号 11→12px、颜色加深。
- **配色方案 variant**：3 套方案（紫/绿/橙）的 bg/accent 全是 `#2f6f3a`（紫=绿，无差异）→ 紫改为 `#663af3`/`#a78bfa`，绿保持，橙改为 `#cb5521`/`#f59e42`。
- **对齐/打乱 alignSnap**：默认 `render(false)` 显示打乱状态，但「对齐」按钮是 active → 改为 `render(true)` 使状态匹配。
- **SmartArt 流程图 visual (data.js)**：`color:#3a8a8a`（青）在 `background:#2f6f3a`（深绿）上对比度极低 → 文字改 `#ffffff` 加粗；箭头颜色也加深。
- **data.js 额外修复**：L408-410 三处 `#3a8a8a` 浅色文字→`#1a3300`；L602「显示答案」绿底按钮文字→白；L2054 导出视频播放按钮▶→白。
- 156 张缩略图重生成，verify **16/16 PASS**。⚠️ 仅本地未部署。

### 08:19 — 按钮默认态错误 + 演讲者视图内容缺失修复
- **按钮默认 active 与初始渲染不匹配（7 处）**：
  - `revealSteps` ▶播放、`coverBuild` ▶播放、`morphDemo` ▶播放：初始 render(0)=空，去掉 active；同时给 onclick 加 active 切换逻辑
  - `smartartBuild` 添加节点：n=0 空状态，去掉 active
  - `replaceFont` 替换字体：render(false)=原文态，去掉 active
  - `designerDemo` 生成方案：render(false)=清空态，去掉 active
  - `narrateDemo` 停止：未录制无运行态，去掉 active
- **演讲者视图 presenterView 内容缺失**：主幻灯片预览区原为 `rgba(255,255,255,.06)` 空 div（白底全透明不可见）→ 改为 `#f8f6f0` 可见底色 + 内部补充标题栏("📊 季度汇报") + 4 条正文行 + 小图表占位符，完整模拟幻灯片内容。
- verify **16/16 PASS**。⚠️ 仅本地未部署。

### 09:11 — PPT技巧 全量 78 demo 矫正 pass（案例可见性/按钮逻辑/文案可见性）
- **方法**：用 Playwright 逐个挂载全部 78 个 DEMOS 函数，检测可见「笔墨」元素数（ink）；对最低 ink 的 17 个单独大图核查；生成 78 张联系表。同时跑自动对比度扫描（浅色文字/深底深字 0 命中）。
- **按钮逻辑（全局）**：审计发现 41 个 demo 初始带 `active` 高亮。按用户「默认未选中」要求，**全局移除所有 `demo-btn` 初始 active**（Edit replace_all `class="demo-btn active"`→`class="demo-btn"`）。点击处理器均用 classList.add/remove/toggle('active')，不依赖初始 active 状态，移除安全。
- **案例可见性（系统性根因修复）**：发现大量 demo 的幻灯片区用 `rgba(255,255,255,.04~.16)` 背景 + `rgba(255,255,255,.25)` 边框——在白底上近不可见，表现为「案例缺内容」（与 presenterView 同类）。
  - 写 Python 脚本批量将这类近透明白色背景→可见浅绿 `rgba(213,245,194,.30~.55)`；边框→可见绿 `rgba(47,111,58,X)`。
  - 关键坑：这些 rgba 多数**漏写闭合 `)`**（如 `rgba(255,255,255,.2;border-radius` 而非 `rgba(255,255,255,.2);border-radius`），正则需匹配 `)`/`;`/`"` 三种结尾符。
  - 额外修复早期坏编辑残留的嵌套损坏：`rgba(255,255,255,rgba(47,111,58,X)` → `rgba(47,111,58,X)`（3 处）+ `revealSteps` rsSlide 边框。
  - `dataBars` 动态拼接 `background:rgba(255,255,255,'+(i%2?'.03':'.06')` → 改为绿底 `.28/.42`。
  - `pictureCorrect` 暗态渐变 `.08/.04` → `.22/.14`（仍明显比亮态暗，可见）。
- **data.js（缩略图源）扫描**：0 近透明白色、0 浅色文字——缩略图无需改动，无需重生成。
- verify **16/16 PASS** ✅。⚠️ **仅本地未部署**（自上次部署后累积多批修复，待用户确认后统一上线）。
- 本地预览：8021 服务运行中，http://127.0.0.1:8021/ppt-tips/ 返回 200。

### 09:23 — data.js 缩略图根因修复（200 处非法颜色值 → 156 张缩略图重生成）
- **根因**：用户截图显示多个技巧卡片的「改前」缩略图几乎全白/空白。排查发现 **data.js 中存在 200 处非法 CSS 颜色值**：`#777777)`、`#2f6f3a)`、`#cb5521)` —— 颜色值末尾多了一个 `)`，导致浏览器静默忽略这些颜色声明 → 元素完全无背景/无文字颜色 → 白底上不可见。
- **修复**：Python 脚本批量字符串替换：`#777777)`→`#777777`（191 处）、`#2f6f3a)`→`#2f6f3a`（5 处）、`#cb5521)`→`#cb5521`（4 处）。
- **重生成**：跑 `gen_thumbs.js`（Playwright）重新生成全部 **156 张 PNG**（78 技巧 × before/after）。逐个验证截图确认之前空白的 6 个（文字转形状/三步美化图表/组合图柱线双轴/Excel数据嵌入/地图区域着色/平滑切换）现在内容清晰可见。
- **残留**：70 处 `rgba(216,236,248,...)` 浅蓝文字（部分在白底上偏淡），属次要对比度问题，非本次核心故障。
- verify **16/16 PASS** ✅。⚠️ **仅本地未部署**。

### 09:38 — ppthub/demos.js 括号不匹配系统性修复（385 处）
- **用户报告**：ppthub 术语图鉴「参考线」demo 中蓝色方块消失。
- **根因排查**：发现 **ppthub/demos.js 存在与 data.js 同类但更严重的括号 bug**：
  - **304 处** `rgba(...` **漏写闭合 `)`**（如 `rgba(47,111,58,.5`），导致背景色非法被忽略 → 元素不可见
  - **81 处** `#2f6f3a)` 等 hex 颜色**多写 `)`**（与 data.js 同类）
- **修复过程**：首次修复脚本因正则不匹配尾部字符（`'` 单引号、`,` 逗号等）导致部分未修复；二次运行产生**重复叠加损坏**（`.5)47,111,58,.5`）；最终分四轮清理：
  1. 去除 hex 多余 `)`（81 处）
  2. 补全 rgba 闭合 `)`（处理 `'`/`;`/`"`/`>`/`,`/空格 六种结尾符）
  3. 清理修复过程中产生的 34 处重复叠加损坏（两种格式：`.NUM),.NUM)重复` 和 `.NUM)直接重复`）
  4. 修复 1 处模板变量损坏 `${0.25}.25` → `${0.25 + i * 0.12}`
- **最终验证**：unclosed=0, corrupted=0, extra_hex=0, double_comma=0。verify **16/16 PASS**。
- ⚠️ **仅本地未部署**。

### ⚠️ 09:57 更正 — 09:38 那次 ppthub 修复实际把文件写崩了（重大教训）
- **真相**：09:38 的多遍正则修复在 ppthub/demos.js 留下了**语法错误**（如 `rgba(26,51,0,.05))26,51,0,.05)` 重复损坏 + 含模板表达式的 `rgba(${...})` 被误伤）。整文件 JS 解析失败 → `DEMOS` 对象未定义 → **ppthub 全部 183 个「动手试试」挂不上**（用户本地预览看到"全没了"）。
- **为什么没早发现**：`verify_before_deploy.py` 当时只做正则源码检查、**不真正执行 JS**，坏文件也过了 16/16。这是门禁漏洞。
- **正确救法**：从线上干净基线 `https://youkappt.com/ppthub/demos.js`（=上次部署 e997f1c5，语法有效、183 函数）覆盖本地坏文件 → 用**逐字符扫描**（只对「纯数字无模板无括号」的漏闭合 rgba 补 `)`、hex 多余 `)` 直接删）单次修复 → `node --check` 通过 → 浏览器逐个挂载 183+78 demo **0 失败**。
- **SVG 真 bug**：ppthub/demos.js:2121 路径 `d="M40% 40%..."` 用了 `%` 坐标（SVG path 不支持 `%`）→ 加 `viewBox="0 0 100 100"` + 改数字坐标修掉。
- **门禁加固**：`verify_before_deploy.py` 新增「子页 demos/app 语法 (node --check)」关卡（校验 ppthub/ppt-tips 的 demos.js + app.js）。现 **17/17 PASS**。以后批量改 JS 必跑 `node --check`，禁止只靠正则门禁。
- ⚠️ 线上站点（e997f1c5）从未包含这次坏文件，一直正常。所有修复仍**仅本地未部署**。

### 10:14 — ✅ 部署上线（用户授权「部署上线」）
- **门禁**：`verify_before_deploy.py` **17/17 PASS**（EXIT=0），含 node --check 语法关卡。
- **推送**：`/tmp/sync_user_site.py`（EXECUTE=1）走 Git Data API 单 commit，**100 PUT + 0 DEL，仅 1 次 Actions run**。commit `a843f708`，base 于之前线上 HEAD。
- **变更范围**：ppt-tips/data.js、ppt-tips/demos.js、ppthub/demos.js（括号修复+SVG）、156 张缩略图（其中 97 张内容变化）。0 删除，contact/mockup 资产无误排。
- **部署后核验**：`verify_pages_rebuilt.py` ✅ —— Pages workflow run `30322560020` conclusion=success，head `a843f708` == main HEAD。
- **线上抽检**：ppthub/demos.js 字节 262447（与本地一致），参考线修复点 `rgba(47,111,58,.5)` 命中 32 处，`))47` 重复损坏 0 次；youkappt.com / ppt-tips / ppthub 均 HTTP 200。
- **结论**：本轮累积的全部修复（按钮默认态、案例可见性、data.js 200 处非法色、缩略图重生成、ppthub 括号救回+SVG）**已全部上线生效**。

### 10:30 — neat-freak 知识收尾（文档/记忆对齐现役）
- **盘点**：audit-inventory 发现项目根无 CLAUDE.md/AGENTS.md（约定全在 MEMORY.md）；git 本地仅 1 commit `ebde125`（Git Data API 推送不更新本地 git，已知状态）；无残留 worktree；`mockup/.workbuddy/` 嵌套会话残留；根级 verify_*.png×3、__pycache__×2、.DS_Store 残留。
- **MEMORY.md 修正 5 处过期**：①工具数 161→172；②部署门禁 16/16→17/17 + 补 node --check 关卡说明；③ppt-tips「18条/36张」→「78条/156张」（核实 data.js 条目 78、thumbs 156）；④`verify_ppthub.js` 路径精确化为 `ppthub/tests/` + 补 demo_runtime_test.js + 规范指针；⑤新增「批量正则禁多遍叠加」教训段（07-28 重大 bug 根因）。
- **ppthub/docs/自查规范.md**：demo 数字 174→≈184（term 实际 184）。
- **待用户确认删除（未确认未删）**：mockup/.workbuddy/、verify_*.png×3、__pycache__×2、.DS_Store。
- **遗留**：docs/plans 设计稿部分过期（Hero 3卡→4卡，属历史快照建议标注）；上级 .codex/AGENTS.md 硬编码生图 key（超范围未改）。
- **清场执行（用户确认「全部清理」）**：已删 `mockup/.workbuddy/`、`verify_nav_btn.png`、`verify_nav_full.png`、`verify_nav_top.png`、`__pycache__/`、`tests/__pycache__/`、`.DS_Store`。复核：主项目 `.workbuddy/`（MEMORY.md + 日志）完好，无唯一改动被误删。

### 15:48 — 加入新工具 Lovart 到【生图工具】
- `tools.md`：在 `### 生图工具 (TOOLS)` 段（AI生图分类下）新增 `- [Lovart](https://www.lovart.ai/) — 全球首个 AI 设计代理（design agent），一句话生成海报/插画/品牌物料，多风格一体化输出。｜免费`（免费在前，置于即梦AI 之前）。
- `logos/` 增量补图标：`/tmp/fetch_lovart.py` 复用 fetch_logos 逻辑只抓 lovart.ai → `lovart.ai.ico`（15086B，来源 iconhorse），`manifest.json` 更新至 134 条。未跑全量脚本（避免重抓 172 域名）。
- ⚠️ **仅本地改动，未部署**（用户未授权「部署上线」）。本地预览重启 8021 可看效果。

### 15:55 — 付费标签三态细化（免费/每日免费额度/付费）
- **起因**：用户要求把全站二态（免费/付费）细化为三态：`免费`(功能全免费)、`每日免费额度`(有免费额度/次数限制，用完等重置或付费)、`付费`(须订阅/购买才能用核心功能)。
- **tools.md**：脚本 `/tmp/update_tags.py` 按域名集合把 67 个"有免费额度/次数限制"的工具改为 `｜每日免费额度`（含原付费但实为有免费额度的 Looka/标智客/LlamaParse）；Galileo AI 保持 `付费`（免费档近乎不可用）；其余 94 免费 + 12 付费保持。最终 173 条：免费 94 / 每日免费额度 67 / 付费 12。另更新头部格式注释（枚举三态）+ 工具数 172→173。
- **index.html（必修，前端硬编码枚举）**：①解析正则 `(付费|免费)`→`(付费|免费|每日免费额度)`；②`toolTagHtml` 三态 cls（新增 `tag-daily`）；③排序 `rankOf` 三态（免费→每日免费额度→付费）；④CSS 新增 `.tag-daily`（琥珀 `#ffd9a0`，与 mint/blush 协调）。
- **验证**：Playwright 实跑本地 8021 → 徽章 free94/paid12/daily67 与数据源一致，分类内付费全排最后(misorder=0)，控制台 0 错误。
- ⚠️ 仍**仅本地未部署**。待用户授权「部署上线」再推。

### 付费标签新增第四态「每月免费额度」（用户指令）
- 用户要求把"月度额度"工具从「每日免费额度」拆出独立为「每月免费额度」，颜色/层级与「每日免费额度」一致（复用 `.tag-daily` 琥珀色、同中间排序层）。
- 工具：写 `/tmp/split_monthly.py`，按域名集合（strip www.）把 18 个公认月度重置工具改标：Gamma/Tome/Runway/Pika/Luma Dream Machine/Luma Genie/HeyGen/D-ID/ElevenLabs/Uizard/Figma AI/v0.dev/Canva 可画/Tripo/Meshy/Spline AI/LlamaParse/TinyPNG。
- 标签分布变为：免费 94 / 每日免费额度 49 / 每月免费额度 18 / 付费 12 = 173。
- 前端 `index.html` 三处同步：解析正则加 `每月免费额度`；`toolTagHtml` 每月→`tag-daily`；`rankOf` 每月同映射 1（与每日同层）。
- 验证：Playwright 实跑本地 8021，徽章 94/49/18/12 与数据源一致、37 个 `.grid` 排序零错乱、控制台 0 错误。
- 文档：tools.md 头注释与 MEMORY.md 工具数/标签约定更新为四态。
- ⚠️ 仅本地未部署。月度集合基于产品常识判断，若用户认为有遗漏/误判需调整。

### 用户纠正：标签周期必须查官网，且区分"付费月重置"vs"免费月重置"
- 用户指出两层错：①周期判定不能靠常识，须查官网真实信息；②"付费订阅每月重置额度"与"免费档每月重置额度"是完全不同的概念。
- 先回退错误拆分的 19 处「每月免费额度」→「每日免费额度」。
- 逐站 WebFetch 18 个工具官网 pricing 页核实免费档额度与重置周期，按真实信息重标：
  - 每月免费额度(免费档每月重置)：Pika(80/月)、HeyGen(3视频/月)、ElevenLabs(10k/月)、Uizard(3次/月)、v0($5 credits/月)、Tripo(200/月)、Meshy(100/月)、LlamaParse(10K/月)、Canva(AI额度，官网页未写周期但为月度，待确认)= 9 个
  - 付费(无真免费档/仅试用)：Luma(两条同域名,仅$30+/月)、D-ID(仅14天试用)= 3 个
  - 免费(一次性/有限非周期)：Gamma(400一次性)、Runway(125一次性)、Spline(有限文件,AI需付费)= 3 个
  - 每日免费额度(保持)：Figma(150 AI credits/日)、Tome(官网404不可达保留)、TinyPNG(官网页未写周期保留)= 3 个
- 最终分布：免费 97 / 每日 52 / 每月 9 / 付费 15 = 173。Playwright 验证：四态徽章与数据源一致、37 grid 排序0错乱、控制台0错误。
- 教训已写入 MEMORY.md「tools.md 付费标签周期判定规则」段（查官网+区分两概念）。
- ⚠️ 仍仅本地未部署。待确认项：TinyPNG 周期未明、Canva 周期官网页未写——保留/暂标，待用户核对官网。

### 用户指令：去掉 Tome
- 从 `### AI PPT 生成 (AIPPT)` 段删除 Tome 条目（原官网404不可达、暂留的每日免费额度）。tools.md 头部计数 173→172，MEMORY.md 规模 173→172。
- 最终分布：免费 97 / 每日 51 / 每月 9 / 付费 15 = 172。Playwright 验证：Tome 不再渲染、37 grid 排序0错乱、控制台0错误。
- ⚠️ 仅本地未部署。

### 部署上线（用户授权「部署上线」）
- 门禁 `verify_before_deploy.py` 17/17 PASS（EXIT=0，实际 172 工具）。
- 经 Git Data API 单 commit `4b8fd035`（base a843f708），4 PUT + 0 DEL = 仅 1 次 Actions run。变更：tools.md（Lovart+四态+去Tome）、index.html（四态枚举）、logos/lovart.ai.ico、logos/manifest.json。
- 部署后核验 `verify_pages_rebuilt.py` ✅：run `30345897468` success，head=4b8fd035。
- 线上抽检：tools.md Tome 命中 0、工具行 172、四态 97/51/9/15 与本地一致；主站/技巧页/术语页/tools.md 均 HTTP 200。
- 至此本期全部累积本地改动（Lovart 加入、付费标签四态化、去 Tome）均已上线。
